id: path-defi
slug: defi
title: DeFi
description: Specialize in decentralized finance on Solana. Understand AMMs, lending protocols, token mechanics, and create DeFi primitives.
tag: DeFi
order: 4
difficulty: advanced
# Held (LX-D1) and emptied by the catalog migration: its former course
# course-defi-on-solana is retired (its one salvageable asset, token-standards,
# became C5 L2). The id is kept — path ids are permanent once live.
# `retired: true` records that the emptiness is permanent (#627). Re-populating
# is allowed: clear `retired` and add a future DeFi course id in the same
# change — the old course id no longer exists.
retired: true
courses: []
