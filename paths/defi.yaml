id: path-defi
slug: defi
title: DeFi
description: Specialize in decentralized finance on Solana. Understand AMMs, lending protocols, token mechanics, and create DeFi primitives.
tag: DeFi
order: 4
difficulty: advanced
# Held for launch (LX-D1). The app hides a shelf only when `courses` is empty
# (the runtime never reads `draft`), so the hold = draft + empty courses, same
# shape as the other held paths. To un-hold: drop `draft`, restore
# `courses: [course-defi-on-solana]`.
draft: true
courses: []
