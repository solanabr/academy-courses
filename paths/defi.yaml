id: path-defi
slug: defi
title: DeFi
description: Specialize in decentralized finance on Solana. Understand AMMs, lending protocols, token mechanics, and create DeFi primitives.
tag: DeFi
order: 4
difficulty: advanced
# Held (LX-D1) and now also emptied by the catalog migration: its former course
# course-defi-on-solana is retired (its one salvageable asset, token-standards,
# became C5 L2). The app hides a shelf only when `courses` is empty (the runtime
# never reads `draft`), so the hold = draft + empty courses. Re-populate only
# with a future DeFi course id — the old one no longer exists.
draft: true
courses: []
