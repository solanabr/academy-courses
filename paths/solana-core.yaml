id: path-solana-core
slug: solana-core
title: Solana Core
description: The complete path from zero to Solana developer. Start with the fundamentals, understand how the network works, and build your first on-chain program.
tag: Foundation
# order 8 (was 1): the retired shelf must not collide with its successor —
# path-zero-to-deployed holds order 1 / the Foundation slot. If this shelf is
# ever revived it re-earns a real position then.
order: 8
difficulty: beginner
# Retired (LX-D1): superseded by path-zero-to-deployed, which fixes this path's
# ordering error (capstone directly after fundamentals, skipping Rust/Anchor).
# The id is kept — path ids are permanent once live. `retired: true` records
# that the emptiness is permanent rather than "coming soon" (#627).
retired: true
courses: []
