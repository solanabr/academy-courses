id: path-frontend
slug: frontend
title: Frontend
description: Build production-ready Solana dApp frontends. Master wallet integration, on-chain data reading, and interactive transaction UIs.
tag: UI/UX
order: 3
difficulty: intermediate
# Retired in the catalog migration: its only course, course-solana-frontend, is
# retired, and its successor material lives inside C4 (course-dapp-sdk-kit) on
# path-zero-to-deployed. Id kept — never delete a live path id. Emptying
# `courses` removes the shelf from the catalog; `draft` keeps the empty shelf
# schema-legal.
draft: true
courses: []
