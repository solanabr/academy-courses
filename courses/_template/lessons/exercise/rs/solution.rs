// A graded Rust file exposes exactly ONE bare column-0 `fn` — the entry point the
// grader calls. Helpers go `const fn` or nested. CONTRIBUTING.md § "Graded Rust: the one-fn rule".
fn add(a: i64, b: i64) -> i64 { a + b }
