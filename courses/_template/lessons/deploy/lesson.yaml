id: lesson-t-deploy
slug: deploy
title: Deploy
skills:
  - anchor
  - program-deployment
blocks:
  - key: build
    type: code
    language: rust
    buildType: buildable
    deployable: true
    starter: program/starter.rs
    solution: program/solution.rs
    tests: program/tests.json
    consumes: [funded-wallet]
    produces: deployed-program
  # The deploy panel is mounted by this block and nothing else, and Submit only
  # appears once the panel reports a successful deploy. A deployable block
  # without it is a lesson nobody can finish.
  - key: deployed
    type: deployed-program-card
    consumes: [deployed-program]
