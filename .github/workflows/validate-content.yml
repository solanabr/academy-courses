name: validate-content

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  content-lint:
    name: content-lint
    uses: solanabr/superteam-academy/.github/workflows/validate-content.yml@main
